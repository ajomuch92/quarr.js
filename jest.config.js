/** @type {import('ts-jest').JestConfigWithTsJest} */
module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    roots: ['<rootDir>/src'], // Cambia a donde tengas tu código fuente
    moduleFileExtensions: ['ts', 'js'],
  };
  